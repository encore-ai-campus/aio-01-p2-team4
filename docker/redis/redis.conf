appendonly yes
appendfsync everysec
# Docker 호스트의 Backend가 포트 포워딩으로 접속하므로 protected mode를 끈다.
# compose에서는 호스트 loopback에만 포트를 바인딩해 외부 네트워크 노출을 막는다.
protected-mode no
save 900 1
save 300 10
save 60 10000
