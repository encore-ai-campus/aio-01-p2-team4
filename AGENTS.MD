# AGENTS 작업 규칙

이 문서는 이 저장소에서 사람과 자동화 에이전트가 반드시 따라야 하는 작업 규칙입니다. 아래 규칙은 권고가 아니라 필수입니다.

## AI 마피아 MVP 정본

AI 마피아 MVP 구현은 3인이 Front / Backend / MCP Server·Data Infrastructure
섹터로 나누어 별도 시스템에서 개발한 뒤 merge합니다. 각 섹터 작업은 착수 전에 이
문서와 다음 정본 중 작업에 관계된 문서를 반드시 읽어야 합니다.

- 공통 제품 규칙·시나리오·소유권·WU·CP:
  [AI_MAFIA_MASTER_PLAN.md](docs/개발상세플랜/01_core/AI_MAFIA_MASTER_PLAN.md)
- PostgreSQL·Redis·transaction·migration:
  [AI_MAFIA_DB_DESIGN.md](docs/개발상세플랜/02_backend_data/AI_MAFIA_DB_DESIGN.md)
- 공개·내부·MCP API:
  [AI_MAFIA_API_SPEC.md](docs/개발상세플랜/01_core/AI_MAFIA_API_SPEC.md)
- MCP runtime 구현 구조·WU 실행 설계:
  [AI_MAFIA_MCP_SERVER_DESIGN.md](docs/개발상세플랜/03_mcp_agent/AI_MAFIA_MCP_SERVER_DESIGN.md)
- 사용자·관리자 화면:
  [AI_MAFIA_SCREEN_FLOW.md](docs/개발상세플랜/04_frontend/AI_MAFIA_SCREEN_FLOW.md)

다섯 MCP Resource의 상세 `data` schema는 API 명세 8.2절과 그 절이 명시적으로
참조하는 API 공통 모델만 정본이다. MCP 서버 설계서에는 URI·Engine scope 매핑과
예시만 두며 별도 schema를 복제하지 않는다.

**coding AI agent에게 한 번에 모든 구현을 지시하는 것을 금지**하며, 한 세션의
범위는 마스터플랜에 정의된 작업 단위(WU) 1개 이하로 제한합니다. 작업 범위나 신규
파일이 정본과 다르면 구현 전에 관련 정본을 갱신하고 섹터 간 합의를 마쳐야 합니다.
정본과 이 문서가 충돌하면 이 문서가 우선합니다.

Backend는 DB schema·migration·repository와 Redis application 코드를 소유하고,
MCP 섹터는 PostgreSQL·Redis 실행 환경의 구축·기동·중지, 접속 계정·권한 준비,
migration 실행과 health 확인을 담당합니다. MCP 서버 runtime은 DB·Redis에 직접
접근하지 않으며 Backend 내부 API만 호출합니다.

## 브랜치와 Git 작업

1. **`main` 브랜치에 직접 커밋하지 말고 `origin/main`에 직접 푸시하지 마세요.** 커밋이 필요한 작업은 별도의 작업 브랜치에서 수행하고 검토 절차를 거쳐 반영해야 합니다.
2. **사용자가 명시적으로 승인하지 않은 커밋은 절대 만들지 마세요.** 구현 요청은 파일 변경을 승인하지만 커밋이나 푸시까지 자동으로 승인하지 않습니다.
3. 커밋 전에 현재 브랜치와 변경 파일을 확인하고, 사용자 소유의 기존 변경을 덮어쓰거나 되돌리지 마세요.
4. `git reset --hard`, 강제 푸시, 광범위한 파일 삭제처럼 복구하기 어려운 명령은 명확한 사용자 요청과 정확한 대상 확인 없이 실행하지 마세요.

## 커밋 메시지

**커밋 메시지는 변경 목적, 영향 범위, 검증 결과를 다른 개발자가 이해할 수 있을 정도로 상세히 작성해야 합니다.** 제목만으로 끝나는 모호한 메시지나 `fix`, `update`, `work` 같은 단어만 사용한 메시지는 금지합니다.

커밋 메시지에는 최소한 다음 정보가 드러나야 합니다.

- 왜 변경했는지에 대한 목적
- 어떤 기능·파일·데이터 경계를 변경했는지에 대한 범위
- 실행한 테스트, lint, 수동 확인 또는 검증을 생략한 이유

## 코드와 주석

1. **새로 작성하거나 수정하는 모든 코드 주석과 docstring은 한국어로 상세하고 정확하게 작성하세요.** 구현을 그대로 반복하는 설명이 아니라 의도, 경계 조건, 보안 이유 또는 유지보수 시 주의점을 설명해야 합니다.
2. 기존 구조와 명명 규칙을 우선 따르고, 요청 범위 밖의 대규모 리팩터링을 함께 섞지 마세요.
3. 인증 claim, DB 값, 사용자 입력 등 외부 데이터는 신뢰하지 말고 검증·정규화·escape한 뒤 사용하세요.

## 파일 및 디렉터리 구조

1. **파일 및 디렉터리 구조를 임의로 생성·삭제·이동하거나 이름을 변경하지 마세요.** 요청된 기능을 기존 구조 안에서 구현하는 것을 우선하고, 구조 변경은 구현상 불가피한 최소 범위로 제한해야 합니다.
2. 구조 변경이 불가피하면 작업을 시작하기 전에 변경 대상, 변경 이유 및 예상 영향을 사용자에게 명확히 고지하세요.
3. 구조를 변경한 경우 루트 [README.md](README.md)의 프로젝트 구조와 관련 안내에 변경 내용을 반드시 반영하세요.

## 비밀정보

1. **API 키, OAuth client secret, 쿠키 secret, DB 비밀번호, token, 실제 `.env`, 실제 `secrets.toml` 및 Google OAuth 다운로드 JSON을 절대 커밋하지 마세요.**
2. 비밀값을 소스 코드, 테스트 fixture, 스냅샷, 로그, 오류 메시지, 문서 또는 커밋 메시지에 복사하지 마세요.
3. 예제에는 명확한 placeholder나 synthetic 값만 사용하세요.
4. 비밀값이 노출됐을 가능성이 있으면 값을 다시 출력하지 말고 즉시 사용자에게 알린 뒤 폐기·재발급과 Git 이력 점검을 안내하세요.

## 테스트와 검증

### 기본 DB 작업 대상

- `TEAM_DATABASE_URL`의 팀 DB는 이 프로젝트의 테스트 DB다. DB 조사, 실행 확인,
  실제 게임 및 DB 연동 검증은 팀 DB를 우선 사용한다.
- 사용자 요청 없이 로컬 DB를 새로 만들거나 로컬 DB로 전환하지 않는다. 실제 `.env`에는
  로컬 DB URL을 두지 않으며 `run_openai.sh`의 기본 저장소 모드는 `team`이다.
- 단위 테스트의 mock·fake는 유지한다. 팀 DB를 쓰는 검증에서는 해당 테스트의
  게임·자료만 조작하고 다른 테스트의 데이터나 실행 프로세스를 임의로 정리하지 않는다.

검증 강도는 변경 위험도에 비례시킵니다.

- 문서, 주석, 오탈자 또는 실행 동작에 영향이 없는 스타일 변경은 자동 테스트를 생략할 수 있습니다. 대신 diff, 링크, 명령 및 문서 구조를 직접 확인하세요.
- 한 파일이나 한 기능에 한정된 단순 변경은 가장 작은 관련 테스트, lint, type check 또는 import 확인만 실행하세요.
- 일반 기능 추가나 버그 수정은 개발 중 focused test를 실행하고, 완료 직전에 전체 회귀 테스트를 한 번 실행하세요.
- 인증, 권한, 보안, 결제, 데이터 마이그레이션·삭제, 공개 계약, 동시성처럼 고위험인 변경은 실패·거부 경로를 포함한 테스트를 먼저 또는 함께 작성하고 전체 회귀 테스트를 실행하세요.
- 외부 Cloud API나 유료 API를 사용하는 자동 테스트는 명시적 필요가 없으면 mock·fake·synthetic 데이터로 대체하세요.
- 기존의 무관한 테스트 실패는 원인과 영향만 보고하고 요청 범위 밖에서 임의로 수정하지 마세요.

완료 보고에는 실행한 검증, 결과, 생략한 검증과 그 이유를 짧고 명확하게 포함해야 합니다.

## README 갱신

**모든 작업은 완료 전에 루트 [README.md](README.md)를 반드시 현재 구현과 일치하도록 업데이트해야 합니다. README 갱신을 임의로 생략하지 마세요.**

최소한 다음 중 변경된 내용을 README에 반영하세요.

- 주요 기능과 사용자 동작
- 설치, 환경 변수, 마이그레이션 및 실행 방법
- 외부 서비스와 redirect URI 같은 운영 설정
- 테스트·검증 명령
- 보안 경계와 알려진 제약
- 새로운 디렉터리, 파일 또는 확장 지점

## 완료 점검

작업을 완료하기 전에 다음을 확인하세요.

1. 요청 범위 밖의 파일을 변경하지 않았는가?
2. 비밀정보나 실제 사용자 데이터가 diff에 포함되지 않았는가?
3. 위험도에 맞는 검증을 실행하고 결과를 확인했는가?
4. 루트 README를 현재 구현과 일치하도록 갱신했는가?
5. 파일 또는 디렉터리 구조를 임의로 변경하지 않았으며, 불가피한 변경은 사용자에게 고지하고 README에 반영했는가?
6. 사용자의 명시적 승인 없이 커밋하거나 푸시하지 않았는가?
